# Database
DATABASE_URL=postgresql://fluency_user:PASSWORD@localhost:5432/fluency_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRATION=15m
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here
JWT_REFRESH_EXPIRATION=7d

# Auth Role Cookie (HMAC para validação de role na edge do Next.js)
# Se não definido, usa fallback para JWT_SECRET (mesmo valor nos dois projetos)
AUTH_ROLE_COOKIE_SECRET=your_super_secret_role_cookie_key_here

# App
NODE_ENV=development
APP_PORT=3001
